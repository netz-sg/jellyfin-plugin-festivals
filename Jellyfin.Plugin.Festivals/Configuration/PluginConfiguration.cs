using MediaBrowser.Model.Plugins;

namespace Jellyfin.Plugin.Festivals.Configuration;

/// <summary>
/// Plugin configuration. The festival data itself lives in a separate JSON store,
/// so this only holds plugin-level settings.
/// </summary>
public class PluginConfiguration : BasePluginConfiguration
{
}
